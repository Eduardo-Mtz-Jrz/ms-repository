spring:
  datasource:
    url: jdbc:mysql://uat-server.empresa.com:3307/productos_uat
    username: uat_user
    password: ${DB_PASSWORD}    # ← NUNCA hardcodear passwords
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: validate        # ← Solo valida, NO modifica tablas
    show-sql: false

logging:
  level:
    com.empresa.proyecto: WARN  # ← Solo warnings y errores